{
  "name": "set-tokens",
  "author": "David Xu",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node ./build.js",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "style-dictionary": "^2.8.2"
  }
}
